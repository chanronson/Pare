name: "dummy_project"
version: "1.0.0"
config-version: 2
profile: "dummy_profile"
model-paths: ["models"]
target-path: "target"
clean-targets: ["target"]
models:
  dummy_project:
    example:
      +materialized: view
